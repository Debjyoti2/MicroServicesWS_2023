spring:
  application:
    name: api-gateway
  profiles:
   active: dev
  config:
   import: optional:configserver:http://localhost:8006/configServer
  
  
  management:
    endpoints:
     web:
      exposure:
       include: "*" 
 
  
          

            
      