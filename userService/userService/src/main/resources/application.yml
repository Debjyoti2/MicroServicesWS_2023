spring:
  profiles:
    active: dev
  application:
    name : user-service
  config:
   import: optional:configserver:http://localhost:8006/configServer

management:
    endpoints:
     web:
      exposure:
       include: "*"